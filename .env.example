# Web server (3000 often busy — default in code is 3080)
PORT=3080

# --- IMAP (poller) ---
IMAP_HOST=
IMAP_PORT=993
IMAP_USER=
IMAP_PASSWORD=
IMAP_FOLDER=Leads

# --- Lead extraction ---
# anthropic | openai-compatible (DeepSeek, OpenRouter, OpenAI, etc.)
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=

# If LLM_PROVIDER=openai-compatible (e.g. DeepSeek: base https://api.deepseek.com , model deepseek-chat)
OPENAI_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=
LLM_MODEL=deepseek-chat

# --- Google (Sheets + optional Calendar/Gmail via same OAuth token) ---
GOOGLE_SPREADSHEET_ID=
GOOGLE_ARCHIVE_SPREADSHEET_ID=
GOOGLE_CALENDAR_ID=primary

MY_NAME=
MY_EMAIL=
MY_PHONE=
EMAIL_SIGNATURE=

# Admin API: POST /api/admin/vertriebler with header Authorization: Bearer <token>
ADMIN_TOKEN=

# Fallback if data/vertriebler.json does not exist yet (comma-separated)
VERTIEBER_NAMES=
